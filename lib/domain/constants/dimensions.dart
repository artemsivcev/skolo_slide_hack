//Tiles
/// Corned radius when it is not set on the arena.
const double notInstalledTileCornerRadius = 35.0;

/// Corned radius when it set on the arena.
const double setTileCornerRadius = 15.0;

const double noInstalledTileBlowSize = 0.0;
const double setTileBlowSize = 70;
